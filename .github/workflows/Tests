name: Final Check
on: [push]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Final Test
        run: echo "✅ Все работает! Yandex Action выполняет проверку задания"
