name: Debug
on: [push]
jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test basic commands
        run: |
          echo "Step 1 - OK"
          ls -la
          echo "Step 2 - OK"
